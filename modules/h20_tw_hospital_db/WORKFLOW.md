# 🤖 `M05 tw-hospital-db` AI Agent 操作導引工作流 (Agent Workflow)
* **模組代號**：`M05` (`tw-hospital-db`)
* **核心意圖**：WKT 空間半徑檢索、21 時段看診點陣與醫院專科處置能力矩陣對合。
```bash
tw-hospital-cli query --lat 25.03 --lng 121.56 --radius 3km --json
```
